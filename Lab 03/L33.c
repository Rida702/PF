#include<stdio.h>    
int main()

 {  int x;
 	printf("Enter salary:");
 	scanf("%d,x");
 	
 	if (salary<=20000);
 	else if(>20000 and <= 30000);
 	else if(> 30000 and <= 40000);
 	else if(> 40000 and <= 50000);
 	else if(> 50000 and <= 60000 );
 	else if(> 60000);
 	
 	return 0;  
}
